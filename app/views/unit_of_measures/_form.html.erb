<%= form_for(@unit_of_measure) do |f| %>
  <% if @unit_of_measure.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@unit_of_measure.errors.count, "error") %> prohibited this unit_of_measure from being saved:</h2>

      <ul>
      <% @unit_of_measure.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :uom %><br />
    <%= f.text_field :uom, :size => 10 %>
  </div>
  <div class="field">
    <%= f.label :uom_desc %><br />
    <%= f.text_field :uom_desc, :size => 30 %>
  </div>
  <div class="field">
    <%= f.label :comments %><br />
    <%= f.text_area :comments, :rows => 5, :cols => 25 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
