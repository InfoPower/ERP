<!DOCTYPE html>
<html>
 <head>
    <title>Infotree</title>
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>

<script>
      
  $(document).ready(function(){

$("li.h").hover(
      function () {
     //   $(this).append($("<span>***</span>"));
         var t = $(this).attr('id');
         if (t.substr(0,1) == "C") {
            var l = t.substr(5);
        		}
         else
           {
            var l = t.substr(6);
           }
          var x = 'li.#' + l;
         $(this).prop('title', $(x).text());
      //   alert($(x).text());
      }, 
      function () {
        $(this).find("span:last").remove();
      }
    );


    $("#menu")
        .jstree({ "plugins" : ["themes","html_data","ui","cookies"],
                  "cookies" : { "save_selected": false}
          })
        .bind("select_node.jstree", function (event, data) {
	    var loc = data.rslt.obj.attr("href");
            window.location = loc;
	           
            var parid = '#' + data.rslt.obj.attr("id");
            var c = data.rslt.obj.attr("id").substr(0,1);
            
		
		
//            if (c == "C")
//             {
//             
//              window.location = loc;
//            
//         
//                 }
//           else
//		{
//
//                
//              $('#menu').jstree("toggle_node",parid);
//              
//
//		}
            
	    
        })
});

    </script>

 </head>
  <body id="store" >
    <div id="banner" >
      <%= image_tag("info.png") %>
      <%= "InfoPower International" %>
    </div>
    <div id="columns" >
      <div id="lside" >
        <div id="menu">
         <%= render "pages/page" %>
        </div>
      </div>
      <div id="main" >
        <%= yield %>
      </div>
      <div id="rside">
       <div id="submenu">
         <%= render "pages/rsubpage" %>
       </div>
      </div>
    </div>
  </body>
</html>